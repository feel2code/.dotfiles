# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1
# set vi in copy-mode
set-window-option -g mode-keys vi
# colors
set -g status-bg color97
set -g status-fg color255
# utc time
set -g status-right '#(TZ="Europe/London" date +"%a %d %b %Y %%H:%%M")'
